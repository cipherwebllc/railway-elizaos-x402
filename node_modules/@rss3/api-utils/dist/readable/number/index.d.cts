declare function formatTokenValue(amount_?: string | null, decimals?: number): string;
declare function formatUnits(bigNum: string, unit: number): string;
declare function formatThousands(num: number): string;
declare function formatPrecision(num: number, n?: number): number;

export { formatPrecision, formatThousands, formatTokenValue, formatUnits };
