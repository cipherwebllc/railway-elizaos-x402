import { TokenType, Token } from './token.cjs';
import '@rss3/api-core';

type Theme<T> = {
    [key in TokenType]: (content: string, meta?: Token["meta"]) => T;
};
declare const themePlain: Theme<string>;

export { type Theme, themePlain };
