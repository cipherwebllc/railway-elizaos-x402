'use strict';

var index_js = require('../../metadata/index.js');

function extractAsset(action) {
  let res;
  index_js.handleMetadata(action, {
    "collectible-transfer": (m) => {
      res = extractNFT(m);
    },
    "collectible-approval": (m) => {
      res = extractNFT(m);
    },
    "collectible-mint": (m) => {
      res = extractNFT(m);
    },
    "collectible-burn": (m) => {
      res = extractNFT(m);
    },
    "collectible-trade": (m) => {
      res = extractNFT(m);
    }
  });
  return res;
}
function extractNFT(m) {
  return {
    address: m.address,
    id: m.id,
    url: m.parsedImageUrl,
    title: m.name,
    standard: m.standard
  };
}

exports.extractAsset = extractAsset;
exports.extractNFT = extractNFT;
//# sourceMappingURL=index.cjs.map
//# sourceMappingURL=index.cjs.map