import { handleMetadata } from '../../metadata/index.js';

function extractAsset(action) {
  let res;
  handleMetadata(action, {
    "collectible-transfer": (m) => {
      res = extractNFT(m);
    },
    "collectible-approval": (m) => {
      res = extractNFT(m);
    },
    "collectible-mint": (m) => {
      res = extractNFT(m);
    },
    "collectible-burn": (m) => {
      res = extractNFT(m);
    },
    "collectible-trade": (m) => {
      res = extractNFT(m);
    }
  });
  return res;
}
function extractNFT(m) {
  return {
    address: m.address,
    id: m.id,
    url: m.parsedImageUrl,
    title: m.name,
    standard: m.standard
  };
}

export { extractAsset, extractNFT };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map