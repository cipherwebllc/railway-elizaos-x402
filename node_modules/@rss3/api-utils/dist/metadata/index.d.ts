import { Action } from '@rss3/api-core';
import { MetadataDoc } from './doc.js';
export { renderItemActionToHTML } from './parser.js';

type Handlers = {
    [K in keyof MetadataDoc]: (metadata: MetadataDoc[K]["ref"]) => void;
};
declare function getTagType(action: Action): keyof Handlers;
declare function handleMetadata(action: Action, handlers: Partial<Handlers>): void;

export { getTagType, handleMetadata };
