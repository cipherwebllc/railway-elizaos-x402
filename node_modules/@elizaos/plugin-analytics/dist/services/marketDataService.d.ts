import type { IAgentRuntime } from '@elizaos/core';
import { Service } from '@elizaos/core';
export declare class MarketDataService extends Service {
    private isRunning;
    static serviceType: string;
    capabilityDescription: string;
    private birdeyeProvider?;
    private coinmarketcapProvider?;
    constructor(runtime: IAgentRuntime);
    getMarketData(chain?: string): Promise<import("../interfaces/types").MarketAnalytics | null>;
    getTokenPrice(tokenAddress: string, chain?: string): Promise<import("../interfaces/types").TokenPriceData | null>;
    /**
     * Start the scenario service with the given runtime.
     * @param {IAgentRuntime} runtime - The agent runtime
     * @returns {Promise<ScenarioService>} - The started scenario service
     */
    static start(runtime: IAgentRuntime): Promise<MarketDataService>;
    /**
     * Stops the Scenario service associated with the given runtime.
     *
     * @param {IAgentRuntime} runtime The runtime to stop the service for.
     * @throws {Error} When the Scenario service is not found.
     */
    static stop(runtime: IAgentRuntime): Promise<void>;
    start(): Promise<void>;
    stop(): Promise<void>;
    isServiceRunning(): boolean;
}
//# sourceMappingURL=marketDataService.d.ts.map