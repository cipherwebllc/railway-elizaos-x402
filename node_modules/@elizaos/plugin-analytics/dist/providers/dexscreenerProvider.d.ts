import type { IAgentRuntime } from '@elizaos/core';
import type { TokenPriceData, HistoricalPriceData, MarketAnalytics, AccountAnalytics } from '../interfaces/types';
/**
 * DexScreener Data Provider
 * Integrates with DexScreener API for multi-chain token data
 */
export declare class DexScreenerProvider {
    private runtime;
    private baseUrl;
    constructor(runtime: IAgentRuntime);
    /**
     * Get token price data from DexScreener
     */
    getTokenPrice(tokenAddress: string, chain?: string): Promise<TokenPriceData | null>;
    /**
     * Get historical price data from DexScreener
     */
    getHistoricalData(tokenAddress: string, chain?: string, timeframe?: string): Promise<HistoricalPriceData[]>;
    /**
     * Get market data from DexScreener
     */
    getMarketData(chain?: string): Promise<MarketAnalytics | null>;
    /**
     * Get account/wallet data from DexScreener
     * Note: DexScreener doesn't provide wallet portfolio data directly
     */
    getAccountData(walletAddress: string, chain?: string): Promise<AccountAnalytics | null>;
    /**
     * Search for tokens by name or symbol
     */
    searchTokens(query: string, chain?: string): Promise<TokenPriceData[]>;
    /**
     * Get interval in milliseconds for timeframe
     */
    private getIntervalMs;
    /**
     * Get number of data points for timeframe
     */
    private getDataPointsForTimeframe;
    /**
     * Get cached data
     */
    private getCachedData;
    /**
     * Set cached data
     */
    private setCachedData;
}
//# sourceMappingURL=dexscreenerProvider.d.ts.map