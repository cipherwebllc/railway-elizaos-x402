/**
 * Date filter types and utilities
 */
export interface DateFilter {
    startDate?: Date;
    endDate?: Date;
    period?: string;
    type: 'range' | 'period' | 'since' | 'until';
}
/**
 * Parse date filter from natural language message
 */
export declare function parseDateFilterFromMessage(messageText: string): DateFilter | null;
/**
 * Format date filter as human-readable text
 */
export declare function formatDateFilterText(filter: DateFilter): string;
/**
 * Check if a date is within the filter range
 */
export declare function isDateInFilter(date: Date, filter: DateFilter): boolean;
//# sourceMappingURL=dateFilter.d.ts.map