/**
 * Technical Analysis Utilities
 * Comprehensive collection of technical indicators for cryptocurrency analysis
 */
/**
 * Calculate Simple Moving Average (SMA)
 */
export declare function calculateSMA(prices: number[], period: number): number[];
/**
 * Calculate Exponential Moving Average (EMA)
 */
export declare function calculateEMA(prices: number[], period: number): number[];
/**
 * Calculate MACD (Moving Average Convergence Divergence)
 */
export declare function calculateMACD(prices: number[], fastPeriod?: number, slowPeriod?: number, signalPeriod?: number): {
    macd: number[];
    signal: number[];
    histogram: number[];
};
/**
 * Calculate RSI (Relative Strength Index)
 */
export declare function calculateRSI(prices: number[], period?: number): number[];
/**
 * Calculate Bollinger Bands
 */
export declare function calculateBollingerBands(prices: number[], period?: number, standardDeviations?: number): {
    upper: number[];
    middle: number[];
    lower: number[];
    bandwidth: number[];
    percentB: number[];
};
/**
 * Calculate Volume indicators
 */
export declare function calculateVolumeIndicators(prices: number[], volumes: number[], period?: number): {
    volumeSMA: number[];
    volumeRatio: number[];
    onBalanceVolume: number[];
};
/**
 * Calculate Stochastic Oscillator
 */
export declare function calculateStochastic(highs: number[], lows: number[], closes: number[], kPeriod?: number, dPeriod?: number): {
    k: number[];
    d: number[];
};
/**
 * Calculate Average True Range (ATR)
 */
export declare function calculateATR(highs: number[], lows: number[], closes: number[], period?: number): number[];
/**
 * Calculate Williams %R
 */
export declare function calculateWilliamsR(highs: number[], lows: number[], closes: number[], period?: number): number[];
/**
 * Calculate Commodity Channel Index (CCI)
 */
export declare function calculateCCI(highs: number[], lows: number[], closes: number[], period?: number): number[];
/**
 * Calculate Money Flow Index (MFI)
 */
export declare function calculateMFI(highs: number[], lows: number[], closes: number[], volumes: number[], period?: number): number[];
/**
 * Calculate Parabolic SAR
 */
export declare function calculateParabolicSAR(highs: number[], lows: number[], accelerationFactor?: number, maximumAcceleration?: number): number[];
/**
 * Calculate Average Directional Index (ADX)
 */
export declare function calculateADX(highs: number[], lows: number[], closes: number[], period?: number): {
    adx: number[];
    plusDI: number[];
    minusDI: number[];
};
/**
 * Generate trading signals based on technical indicators
 */
export declare function generateSignals(prices: number[], volumes: number[], highs: number[], lows: number[]): {
    macdSignal: 'buy' | 'sell' | 'hold';
    rsiSignal: 'buy' | 'sell' | 'hold';
    bbSignal: 'buy' | 'sell' | 'hold';
    volumeSignal: 'buy' | 'sell' | 'hold';
    overallSignal: 'buy' | 'sell' | 'hold';
    confidence: number;
};
//# sourceMappingURL=technicalAnalysis.d.ts.map