import type { IAgentRuntime, Memory } from '@elizaos/core';
/**
 * Extract wallet addresses from text
 * Supports Solana (base58) and EVM (hex) addresses
 */
export declare function getWalletsFromText(runtime: IAgentRuntime, message: Memory): Promise<string[]>;
/**
 * Get account information from message context
 * Simplified version that extracts basic user info
 */
export declare function getAccountFromMessage(runtime: IAgentRuntime, message: Memory): Promise<{
    userId?: string;
    walletAddress?: string;
    visualOutput?: boolean;
} | null>;
/**
 * Check if visual output is enabled for the user
 */
export declare function isVisualOutputEnabled(runtime: IAgentRuntime, message: Memory): Promise<boolean>;
//# sourceMappingURL=messageUtils.d.ts.map